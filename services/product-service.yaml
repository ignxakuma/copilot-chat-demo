# Purpose: Deploy and expose the product-service microservice.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: retail-aiops
  labels:
    app: product-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      containers:
        - name: product-service
          image: local/product-service:0.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"
---
# Purpose: ClusterIP service for internal DNS-based discovery of product-service.
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: retail-aiops
spec:
  selector:
    app: product-service
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP

